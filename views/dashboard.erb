<h1>Twitter Search</h1>
<hr>
<h2> Dashboard</h2>
<hr>

<center>
<P><%= "No. of unique users tweeting about '#{params[:keyword]}': #{ tweets.map { |tweet| tweet[:name] }.uniq.count }" %>
<br>
<%= "No. of tweets : #{ tweets.count }" %>
<br>
<%= "No. of re-tweets : #{ retweets.count }" %>
<br><br>

<form id="key_return" action="/results" method="post">
  <input type="hidden" value="tweets" name="tweets_retweets">
  <input type="hidden" value= "<%= "#{key}" %>" name="key">
</form>

<button type="submit" form="key_return" class="button"><span>See all tweets</span></button>
</center>

<h1>Twitter Search</h1>
<hr>
<h2> Dashboard</h2>
<center>
<P><%= p "No. of unique users tweeting about '#{params[:keyword]}': #{ tweets.map { |x| x.user.screen_name }.uniq.count }" %>
<br>
<%= p "No. of tweets : #{ tweets.map { |x| x.text }.count }" %>
<br>
<%= p "No. of re-tweets : #{ retweets.length }" %>
<br><br>

<form id="tweets_json" action="/results" method="post">
	<input type="hidden" value= "<% tweets.to_json %>" name="tweets">
</form>

<button type="submit" form="tweets_json" class="button"><span>See all tweets</span></button>
</center>
